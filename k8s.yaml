apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: nginx-deployment-dev
  namespace: dev
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx:<BUILD_TAG>
          ports:
            - containerPort: 80

